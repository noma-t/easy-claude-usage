{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and the tray context menu window",
  "windows": ["main", "tray-context-menu"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
